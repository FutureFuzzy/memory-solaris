# 记忆星球 Memory Solaris — 产品设计说明书

> 版本：MVP v1.0 | 日期：2025-05-25 | 状态：Demo 可运行

---

## 一、产品定义

### 1.1 一句话定义

将 AI 对你的"记忆黑盒"变成一座可看、可摸、可改的 3D 星系。

### 1.2 核心问题

AI 聊天产品（ChatGPT、Character.AI 等）会在对话中积累对用户的认知，但这些记忆对用户来说是一个**完全不透明的黑盒**——你不知道 AI 记住了什么、记错了什么、不同记忆之间有什么关系。用户对 AI 对自己的认知**毫无掌控感**。

### 1.3 解决思路

记忆星球将 AI 提取的用户事实**可视化为一颗颗 3D 星球**，按语义主题自动聚类成独立星系，让用户：

- **看见** AI 眼中的自己
- **修正** 错误或不想要的记忆
- **关联** 散落的记忆碎片
- **理解** AI 为什么把某些事情联系在一起

---

## 二、设计判断与 Trade-off

这些判断是产品一切实现决策的根基。

### 2.1 记忆应该如何呈现？

**判断：物理隐喻优于抽象图表。**

| 决策 | 采用 | 舍弃 | 原因 |
|------|------|------|------|
| 3D 太阳系 | 星球 + 恒星 + 轨道 | 力导向图 / 思维导图 / 表格列表 | 太阳系是人人理解的物理模型，无需学习即可理解"轨道=时间""恒星=主题""距离=关联度" |
| 行星 = 记忆 | 每条记忆一颗球体 | 列表卡片 / 时间线 | 物理实体唤起"可以触碰、拖拽、扔掉"的直觉 |
| 星系 = 主题 | 语义聚类，自动分群 | 用户手动建文件夹 | 让 AI 做分类，用户做修正。自动聚类降低认知负担 |
| 情感用光晕表达 | 暖金色光晕 = 积极，冷蓝色 = 消极 | 星球本体变色 | 颜色用于区分主题星系，情感作为辅助层叠加，避免颜色信息过载 |

**关于"为什么不用列表"的补充说明**：列表适合精确查找，但不适合"浏览式理解"。用户打开记忆星球的目的是"了解 AI 怎么看我"，这是一个**探索性、全局性**的任务，空间化的视觉呈现比线性列表更符合这个目标。

### 2.2 用户如何管理记忆？

**判断：操作即所见，直接操纵优于菜单命令。**

| 决策 | 采用 | 舍弃 | 原因 |
|------|------|------|------|
| 拖拽关联 | 拖 A 到 B = 建立关联 | 弹窗选择目标 / 输入 ID | 物理直觉：把两个东西放在一起 = 它们有关系 |
| 拖拽删除 | 点击 + 确认按钮（MVP）| 拖出屏幕边缘删除 | MVP 阶段拖出屏幕的 NDC 检测不够可靠，先用明确的删除按钮 |
| 星系迁移 | 下拉菜单切换 | 拖拽星球到另一个恒星 | MVP 阶段星系间距大、拖拽跨星系操作精度不够，下拉更可靠 |
| 取消关联 | 详情面板中点击取消 | 长按连线 / 手势 | MVP 优先保证功能可用性 |

**核心原则：用户应该感觉自己在"整理桌面"而不是"操作数据库"。** 这意味着每一次交互都应该有即时视觉反馈，操作应该可逆（关联可以取消，删除有确认），且系统不应该替用户做不可逆的决定。

### 2.3 哪些内容必须透明？

**判断：AI 的判断依据必须可见，但推理过程可以隐藏。**

| 必须透明（用户可见） | 应该隐藏（内部实现） |
|----------------------|----------------------|
| AI 记住了什么事实（fact 原文） | AI 的原始 token 预测过程 |
| 每条记忆的置信度（百分比） | embedding 向量的具体数值 |
| 每条记忆的情感倾向（积极/消极/中性） | 聚类质心计算过程 |
| 记忆之间的关联关系（引力线） | 力导向布局的迭代计算 |
| 记忆属于哪个主题星系 | 余弦相似度的具体分数 |
| AI 提供商和模型名称 | API Key（脱敏显示） |

**为什么这样划分？** 用户需要知道"AI 认为我 90% 概率喜欢猫"来做判断（"不对，我只是随口提了一句"），但不需要看 384 维浮点数向量。透明度的边界是：**用户能据此做出管理决策的信息，就应该透明。**

### 2.4 哪些内容应该被隐藏？

**判断：技术复杂度默认隐藏，除非用户主动探索。**

| 隐藏项 | 原因 |
|--------|------|
| embedding 生成过程 | Web Worker 在后台运行，用户无感知 |
| 聚类触发时机 | 自动在 embedding 生成后触发，无需用户理解"聚类"概念 |
| 力导向布局计算 | 星系位置自动计算，用户只看到"相似主题靠得近"的结果 |
| IndexedDB 存储细节 | 用户只需知道"数据在本地、刷新不丢失" |
| 模型 API 调用细节 | 封装在 useMemories hook 中，用户只需配置 API Key |

### 2.5 MVP 阶段的重大 Trade-off

以下是对 V1（完整愿景）→ MVP（当前 Demo）之间取舍的最关键判断：

**Trade-off 1：纯浏览器 vs 桌面客户端**

放弃了 Electron/Tauri 桌面客户端方案，选择了纯 Web 应用。
- **得到**：零安装、跨平台、部署简单（一个静态站点）
- **失去**：无法访问本地文件系统、没有系统级快捷键、性能受浏览器限制
- **为什么这样选**：MVP 阶段需要快速迭代和远程演示，Web 应用最灵活。桌面客户端在需要本地 LLM 推理时更有价值。

**Trade-off 2：embedding 本地生成（384 维 MiniLM） vs 调用 API 生成**

选择了在浏览器中运行 all-MiniLM-L6-v2 模型生成 embedding。
- **得到**：零成本、离线可用、隐私安全（文本不出浏览器）
- **失去**：模型精度不如大模型 API（如 text-embedding-3-large），首次加载需下载 ~20MB 模型文件
- **为什么这样选**：语义聚类只需要相对相似度，MiniLM 的 384 维向量精度足够区分不同主题。如果未来需要更精细的聚类，可以升级为 API embedding。

**Trade-off 3：按批次聚类 vs 单条实时聚类**

创建记忆时，一个批次的记忆共享同一个 clusterId；embedding 生成后，再逐条尝试归入已有 cluster。
- **得到**：同一轮对话提取的多条记忆天然聚在一起（避免了"养猫"和"猫粮"被分到不同星系）
- **失去**：同一批次中语义差异很大的记忆不会立即分开（需要 embedding 生成后才触发重聚类）
- **为什么这样选**：实际体验中，同一轮对话提取的 2-3 条记忆通常属于同一主题，批处理减少了不必要的碎片星系。

**Trade-off 4：力导向布局（50 轮迭代） vs 固定圆形布局**

星系位置通过 50 轮力导向迭代计算，而非固定在圆形轨道上。
- **得到**：语义相关的星系自动靠近，形成有意义的空间结构
- **失去**：50 轮迭代在记忆数量多时（>50条）可能产生小卡顿，且布局收敛性不保证
- **为什么这样选**：当前使用场景（<30 条记忆）下性能完全够用，且语义布图是产品的核心差异化视觉特征。

**Trade-off 5：用户 API Key vs 服务端代理**

用户直接在前端配置 API Key，请求直接从浏览器发给 Gemini/DeepSeek。
- **得到**：零服务器成本、零运维、隐私天然保障（API Key 仅存本地 IndexedDB）
- **失去**：API Key 暴露在浏览器 devtools 中（但这是用户自己的 Key）、无法做请求频率控制、无法统一监控
- **为什么这样选**：MVP 面向的是个人用户（自己配置 Key 自己用），不是 SaaS 多租户场景。对于 Demo，这个架构足够且正确。

---

## 三、完整产品架构（V1 目标形态）

```
┌──────────────────────────────────────────────────────┐
│                    记忆星球 (V1)                       │
│                                                      │
│  ┌─────────────┐  ┌─────────────┐  ┌──────────────┐ │
│  │ 前端层       │  │ 本地 AI 层   │  │ 本地存储层    │ │
│  │             │  │             │  │              │ │
│  │ Electron/   │  │ embedding   │  │ SQLite       │ │
│  │ Tauri       │  │ 生成        │  │ (元数据)      │ │
│  │             │  │             │  │              │ │
│  │ Three.js    │  │ 本地 LLM    │  │ LanceDB      │ │
│  │ 渲染引擎    │  │ 推理        │  │ (向量)       │ │
│  │             │  │             │  │              │ │
│  │ React UI    │  │ FSRS 遗忘   │  │ 加密存储     │ │
│  │             │  │ 算法        │  │              │ │
│  └──────┬──────┘  └─────────────┘  └──────────────┘ │
│         │                                            │
│         ▼ (可选)                                      │
│  ┌─────────────┐                                     │
│  │ 云端同步服务 │  FastAPI + PostgreSQL + pgvector     │
│  └─────────────┘                                     │
└──────────────────────────────────────────────────────┘
```

### V1 完整功能模块（12 个）

| # | 模块 | MVP 状态 |
|---|------|----------|
| 1 | 对话记忆提取 | ✅ 已实现 |
| 2 | 3D 太阳系渲染 | ✅ 已实现 |
| 3 | 语义主题聚类 | ✅ 已实现 |
| 4 | 力导向星系布局 | ✅ 已实现 |
| 5 | 记忆关联（引力线） | ✅ 已实现 |
| 6 | 拖拽建立关联 | ✅ 已实现 |
| 7 | 记忆删除 | ✅ 已实现 |
| 8 | 本地持久化 | ✅ 已实现 |
| 9 | 多 AI 提供商 | ✅ 已实现（Gemini + DeepSeek） |
| 10 | 遗忘模拟（FSRS） | ❌ 待实现 |
| 11 | 矛盾检测与提示 | ❌ 待实现 |
| 12 | 手动创建/编辑记忆 | ❌ 待实现 |
| 13 | 记忆搜索与过滤 | ❌ 待实现 |
| 14 | 记忆导出/导入 | ❌ 待实现 |
| 15 | 桌面客户端 | ❌ 待实现 |
| 16 | 云端同步 | ❌ 待实现 |

---

## 四、MVP 当前实现详情

### 4.1 整体架构

```
用户浏览器
├── React 19 + TypeScript + Vite
│   ├── ChatPanel          — 左侧 380px AI 对话框
│   ├── SolarSystem        — 右侧 3D 场景 (Three.js + @react-three/fiber)
│   │   ├── ClusterStar    — 主题恒星（脉动光球）
│   │   ├── OrbitRings     — 轨道环
│   │   ├── Planet         — 记忆行星（拖拽、选中、光晕）
│   │   ├── GravitationLines — 关联引力线（同星系/跨星系）
│   │   └── Scene          — 力导向布局 + Raycaster 交互
│   └── SettingsPanel      — API 配置弹窗
├── useMemories Hook       — 核心业务逻辑
│   ├── sendMessage()      — AI 对话 + 记忆提取
│   ├── deleteMemory()     — 删除记忆 + 清理空星系
│   ├── addMemoryLink()    — 双向关联
│   ├── removeMemoryLink() — 取消关联
│   └── moveMemoryToCluster() — 切换星系
├── Dexie.js / IndexedDB   — 本地持久化
│   ├── memories 表
│   ├── clusters 表
│   └── config 表
└── Web Worker
    └── transformers.js    — all-MiniLM-L6-v2 embedding 生成
```

### 4.2 数据模型

```typescript
// 记忆
interface Memory {
  id: string;           // 唯一标识，客户端生成
  fact: string;         // 一句话事实，如"用户喜欢猫"
  confidence: number;   // 0-1 置信度，AI 判断此事实的确信程度
  sentiment: number;    // -1 到 1 情感值，正值=积极，负值=消极
  createdAt: number;    // 创建时间戳，决定轨道半径
  embedding?: number[]; // 384 维语义向量，Web Worker 异步生成
  accessCount: number;  // 调用次数（预留，当前未使用）
  relatedIds: string[]; // 关联的其他记忆 ID
  clusterId: string;    // 所属星系 ID
}

// 记忆星系（主题集群）
interface MemoryCluster {
  id: string;           // 唯一标识
  label: string;        // 主题名称（从第一条记忆截取前 12 字）
  color: string;        // 星系颜色，8 色调色板循环分配
  createdAt: number;
}

// API 配置
interface ApiConfig {
  provider: 'gemini' | 'deepseek';
  apiKey: string;       // 仅存本地 IndexedDB
  model?: string;       // DeepSeek 可选模型名
}
```

### 4.3 记忆提取流程

```
用户输入文字
  → ChatPanel 调用 sendMessage()
    → 拼接完整对话历史
    → 发送 Gemini/DeepSeek API（带 SYSTEM_PROMPT）
    → AI 回复中包含 [EXTRACTED_MEMORIES]...[/EXTRACTED_MEMORIES] JSON
    → cleanContent() 剥离记忆块，保留干净回复
    → extractMemoriesFromReply() 解析 JSON 数组
    → 创建新 cluster（一批评一批星系的颜色）
    → 提取的记忆批量写入 IndexedDB
    → 每条新记忆 postMessage 到 Web Worker
      → all-MiniLM-L6-v2 生成 384 维向量
      → worker.onmessage 收到 embedding → 写入对应 Memory
      → tryClusterMemory() 计算与所有已有 cluster 质心的余弦相似度
        → 相似度 > 0.45 → 归入该 cluster
        → 所有 cluster 都 < 0.45 → 保持当前 cluster
      → 更新 memories + clusters 状态 → 触发 3D 重渲染
```

### 4.4 3D 渲染系统

**相机**：初始位置 (0, 22, 22)，fov 50，俯视整个星系空间。OrbitControls 支持旋转/缩放。

**场景元素**：

| 元素 | Three.js 实现 | 视觉参数 |
|------|--------------|----------|
| 恒星 | MeshStandardMaterial + 外层 glow mesh | 大小 0.85+正弦脉动，emissive=自身色 |
| 行星 | MeshStandardMaterial（roughness=0.35） | 大小 0.22+confidence×0.6 |
| 光晕 | MeshBasicMaterial（transparent） | 颜色=暖金/冷蓝/灰，强度=0.15+|sentiment|×0.35 |
| 轨道环 | RingGeometry | 半径=3.5+log2(1+天数)×0.9，颜色 #2a2a3e |
| 引力线 | Line + LineBasicMaterial | 同星系靛蓝(#818cf8) op=0.2，跨星系金(#fbbf24) op=0.35 |
| 关联目标环 | RingGeometry | 金色高亮环，半径 1.4-1.55 |

**位置计算**：

```typescript
// 行星轨道位置（极坐标 → 直角坐标）
function getNodePosition(mem, memIndex, memTotal, clusterCenter) {
  const daysAgo = (now - mem.createdAt) / 86400000;
  const radius = 3.5 + log2(1 + daysAgo) * 0.9;  // 对数衰减，防止外围堆积
  const angle = (memIndex / memTotal) * PI * 2;    // 均匀分布
  return clusterCenter + (cos(angle)*radius, 0, sin(angle)*radius);
}

// 星系位置（力导向布局）
function computeClusterPositions(clusters, memories) {
  // Step 1: 计算跨星系语义相似度矩阵（embedding 质心的余弦相似度均值）
  // Step 2: 初始化在半径 10 的圆形上
  // Step 3: 50 轮迭代
  //   - 吸引力：sim × 0.35
  //   - 排斥力：70 / distance²
  //   - 合力缩放：0.12
}
```

### 4.5 交互系统

**已实现**：

| 操作 | 触发方式 | 效果 |
|------|----------|------|
| 旋转/缩放视角 | 鼠标拖拽/滚轮 | OrbitControls 标准操作 |
| 查看详情 | 点击星球 | 弹出详情面板（事实、置信度、情感、星系） |
| 拖拽建立关联 | 按住星球拖到另一个星球 → 松手 | 目标出现金色光环，松手后建立双向关联 |
| 取消关联 | 详情面板中点击取消按钮 | 双向移除关联 |
| 切换星系 | 详情面板下拉菜单 | 记忆迁移到目标星系，原星系为空则自动删除 |
| 删除记忆 | 详情面板中点击删除 | 从 DB 删除，清理空星系，3D 场景即时更新 |

**拖拽关联实现细节**：

```
handlePointerDown → setDragId + setPointerCapture
  → useFrame 循环：
      raycaster 检测 dragPos 与所有其他行星的距离
      → 距离 < 2.8 的最近行星设为 linkTargetId（出现金色光环 + 预览连线）
handlePointerUp → onLink(source, target) → 双向更新 relatedIds
```

### 4.6 聚类系统

**算法流程**：

1. **批次初始化**：同一轮对话提取的记忆共享一个 clusterId
2. **异步聚类**：Web Worker 生成 embedding 后触发 `tryClusterMemory()`
3. **质心比较**：计算目标记忆与所有已有 cluster 的质心（member embeddings 平均值）余弦相似度
4. **阈值匹配**：相似度 > 0.45 → 归入最佳匹配 cluster；否则保持当前 cluster
5. **空星系清理**：cluster 中最后一条记忆被移走/删除时，自动删除 cluster

**聚类算法的设计直觉**：

- 阈值 0.45 经过调优：太高会导致碎片化（同一主题被拆成多个星系），太低会导致粘连（不同主题混在一起）
- 使用质心而非最近邻：避免单个 outlier 记忆影响整个 cluster 的判断
- 只比较"其他 cluster"：新记忆默认已在临时 cluster 中，不会被自己吸引

### 4.7 UI 组件树

```
App (flex h-screen)
├── ChatPanel (w-[380px], 左侧)
│   ├── 标题栏（"AI 对话" + 说明文字）
│   ├── 消息列表（user → 右对齐紫色 / model → 左对齐深色）
│   ├── 加载指示器（Loader2 旋转动画）
│   └── 输入区（textarea 2行 + 发送按钮，Enter 发送）
├── SolarSystem (flex-1, 右侧)
│   ├── Canvas (@react-three/fiber)
│   │   └── Scene
│   │       ├── AmbientLight + PointLight×2
│   │       ├── GravitationLines → LinkLine×N
│   │       ├── ClusterStar + OrbitRings + Planet×M  (per cluster)
│   │       └── 拖拽预览连线
│   ├── 详情面板（左下角浮层，选中记忆时显示）
│   └── 图例面板（左上角浮层，星系颜色 + 操作提示）
└── SettingsPanel (右上角按钮 + 弹窗)
    ├── AI 提供商切换（Gemini / DeepSeek）
    ├── API Key 输入框（password 类型）
    ├── 模型名称输入（DeepSeek 时显示）
    └── 隐私提示
```

### 4.8 颜色系统

**星系调色板（8 色，循环分配）**：

```
琥珀 #f59e0b  翠绿 #10b981  靛蓝 #6366f1  粉 #ec4899
青 #06b6d4    橙 #f97316    紫 #8b5cf6   绿 #14b8a6
```

**情感光晕（叠加在行星外围）**：

```
积极（sentiment > 0.15）  → 暖金 #fbbf24
消极（sentiment < -0.15） → 冷蓝 #60a5fa
中性（-0.15 ~ 0.15）      → 灰 #d4d4d8
```

**关联线**：
```
同星系关联 → 靛蓝 #818cf8，透明度 0.2
跨星系关联 → 金色 #fbbf24，透明度 0.35
拖拽预览线 → 金色 #fbbf24，透明度 0.5
```

**UI 主题色**：

```
背景 #0a0a0f  面板 #1a1a2e  边框 #2a2a3e
主色 #6366f1  hover #818cf8
文字 #e2e8f0  次要文字 #94a3b8
```

---

## 五、待实现模块（V2+）

### 5.1 遗忘模拟（高优先级）

- **目标**：长期未被提及的记忆逐渐缩小、变暗、外移
- **方案**：引入 FSRS（Free Spaced Repetition Scheduler）稳定性/可检索性参数
- **视觉**：行星 size = confidence × stability，stability < 阈值的行星呈现暗淡灰色
- **交互**：用户可"唤醒"即将被遗忘的记忆（点击后重置 stability）

### 5.2 记忆矛盾检测

- **目标**：发现 AI 提取的矛盾事实（如"喜欢猫" vs "对猫毛过敏"）
- **方案**：对 sentiment 符号相反的同一主题记忆做语义相似度计算
- **视觉**：矛盾记忆之间用红色闪烁连线标记
- **交互**：弹出"冲突裁决"面板，用户选择保留/修正/合并

### 5.3 手动创建/编辑记忆

- **目标**：用户可直接创建记忆或修改 AI 提取的内容
- **方案**：详情面板中启用 inline 编辑，底部添加"新建记忆"按钮
- **范围**：fact 文本、confidence、sentiment 均可编辑

### 5.4 搜索与过滤

- **目标**：记忆数量多（>50条）时的快速定位
- **方案**：顶部搜索栏，支持关键词匹配 fact 文本和高亮闪烁对应星球

### 5.5 记忆导出/导入

- **目标**：用户数据可迁移
- **方案**：JSON 格式导出全部 memories + clusters，支持重新导入

### 5.6 桌面客户端

- **目标**：独立窗口运行，系统级快捷键
- **方案**：Electron 或 Tauri 封装，集成本地 LLM 推理

### 5.7 可选云同步

- **目标**：多设备间同步记忆数据
- **方案**：FastAPI + PostgreSQL + pgvector，默认关闭，用户须主动开启

---

## 六、演示脚本

### 6.1 首次使用（Demo 场景）

```
1. 打开应用 → 右侧空白 3D 空间，左侧提示"开始对话"
2. 点击右上角「API 配置」→ 选择 DeepSeek → 输入 API Key
3. 在对话框输入："我最近刚领养了一只橘猫，叫年糕"
   → AI 回复（友好回应 + 提取记忆）
   → 右侧出现第一颗恒星 + 一颗行星（"用户领养了一只橘猫叫年糕"）
4. 继续输入："但我对猫毛有点过敏，早上总打喷嚏"
   → 新增第二颗行星，同星系，光晕偏冷（消极情绪）
5. 输入："我是一名全栈工程师，主要写 React 和 Node.js"
   → 新的主题星系出现！绿色恒星 + 行星（语义距离远，自动独立成系）
6. 拖拽"养猫"星球到"过敏"星球 → 金色连线出现（跨主题关联）
7. 点击星球 → 详情面板 → 查看置信度、情感、可切换星系
8. 刷新页面 → 所有记忆完好恢复
```

### 6.2 Demo 关键卖点

- **实时性**：对话结束星球即出现，embedding 后台异步生成
- **智能聚类**：自动区分"养猫"和"工作"两个主题
- **物理直觉**：拖拽即关联，所见即所得
- **隐私安全**：所有数据在浏览器本地，API Key 不离开用户机器

---

## 七、部署与运维

### 7.1 当前部署

- **地址**：`http://82.156.245.28:3002`
- **服务器**：腾讯云 CVM（Ubuntu, 2C4G）
- **进程管理**：PM2（`memory-solaris`）
- **静态服务**：`serve -s dist/ -l 3002`
- **防火墙**：UFW 3002/tcp 已开放
- **启动恢复**：`pm2 save` 已保存进程列表

### 7.2 本地开发

```bash
npm install
npm run dev       # Vite dev server
npm run build     # 生产构建 → dist/
npm run preview   # 预览构建结果
```

### 7.3 CI/CD 思路（后续）

- GitHub Actions → build → SCP 到服务器 → pm2 restart
- 或使用 Vercel/Cloudflare Pages 直接部署（需注意 embedding worker 的 CSP 配置）

---

## 八、技术债务与已知问题

| # | 问题 | 优先级 | 备注 |
|---|------|--------|------|
| 1 | @xenova/transformers 的 TS 类型不完善 | 低 | 多处使用 `as any`，功能不受影响 |
| 2 | 内存中 embedding 不缓存 | 中 | 每次聚类都需要从 DB 重新读取，记忆多时可能慢 |
| 3 | 力导向布局在 50+ 星系时可能不收敛 | 低 | 当前场景（<10星系）完全够用 |
| 4 | Web Worker 不支持跨域 | 低 | 部署在标准 Web Server 下正常 |
